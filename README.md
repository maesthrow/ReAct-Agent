# ReAct-Agent — «Создай своего AI-агента»

Мини-проект на Python, показывающий эволюцию от простого чата с LLM до агента с инструментами и персистентной памятью. Подходит для обучения, экспериментов и быстрых прототипов.

---

## 🚀 Что внутри

- **`agents/baseline_chat.py`** — простой чат с LLM (без инструментов).
- **`agents/agent.py`** — ReAct-агент через **LangChain**:
  - `search_web(query)` — поиск в DuckDuckGo (ru-RU, свежие результаты);
  - `append_to_file(query, content)` — дописывает конспект в `agent_data/agent.log`.
- **`agents/agent_memory.py`** — тот же агент + **память диалога** на **LangGraph.MemorySaver** (персистентный контекст по `thread_id`).
- **`agents/agent_data/`** — логи и заметки, которые пишет агент.
- **`.env.demo`** — пример настроек окружения.
- **`requirements.txt`** — зависимости.

> Идея: в `agent.py` выбор инструментов делает **LangChain-агент** (ReAct-цикл с tool-calling).  
> В `agent_memory.py` добавлена **персистентная память** через `LangGraph.MemorySaver` (чекпойнтинг), **без** сборки полного графа оркестрации.

---

## 👨‍💻 Стек

- **LLM:** GigaChat (через `langchain_gigachat`)
- **Агент и tool-calling:** **LangChain** (`create_agent`, `@tool`)
- **Память/чекпойнты:** **LangGraph** (`MemorySaver`, `thread_id`)
- **Поиск:** `ddgs` (DuckDuckGo)
- **Конфиги:** `python-dotenv` (.env)
- **Python:** 3.11+ / 3.12

---

## ⚙️ Установка

```bash
git clone <ВАШ-РЕПО> ReAct-Agent
cd ReAct-Agent
python -m venv .venv
# Windows
.venv\Scripts\activate
# Linux/macOS
source .venv/bin/activate

pip install -r requirements.txt
